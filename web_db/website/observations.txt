все работает так:

форма отправляет пост запрос, которые обрабатывается в вьюс, а потом редирект на юзерс => все отрисовывается

нажал на кнопку удалить => js делает delete запрос и ждем когда обработается delete зарос, а потом обновляет страницу на юзерс
------------------
по-хорошему форму нужно тоже сделвть через js запрос
------------------


если делать в ретурне редирект, то происходит редирект с тем же методом

если приходит delete запрос, то результат render-template не отображается (возможно, что фласк по умолчанию не передает ответом body на зпросы типа delete или post)
если приходит post запрос и делать return render_template, то возможен такой сценарий: нажимаем добавить пользователя => он добавдяется и теперь если нажать на значок одновить страницу то произойдет опять post запрос и опять добавится новый юзер. Если возвращать редирект то почему-то срабатывает другой обработчик 
